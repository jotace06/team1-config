apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: course-registration # ← 추가: overlay의 모든 resource에 namespace 적용

resources:
  - ../../base
  - configmap.yaml
  - secret.yaml

labels:
  - pairs:
      environment: dev
    includeSelectors: false

images:
  - name: 582797602058.dkr.ecr.ap-northeast-2.amazonaws.com/course-registration-dev
    newTag: develop-bc2b4e1
